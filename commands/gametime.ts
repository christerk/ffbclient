import Game from "../model/game";

export default class CommandGameTime {
    private controller;

    public constructor(controller: any) {
        this.controller = controller;
    }

    public processCommand(data: any) {
    }
}
